parameters:
  name: ''
  pool: ''
  env: Test

jobs:
- deployment: ${{ parameters.name }}
  pool: ${{ parameters.pool }}
  environment: ${{ parameters.env }}
  strategy:
    runOnce:
      deploy:     
        steps:
          - script: echo 'hello'
